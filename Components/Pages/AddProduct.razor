@inherits LayoutComponentBase
@page "/AddProduct"
@using AmazonSimulatorApp.Data;
@using AmazonSimulatorApp.Services;
@using AmazonSimulatorApp.Data.DTOs;

@inject IProductService ProductService
@inject NavigationManager Navigation

<h3>Add Product</h3>

<EditForm Model="newProduct" OnValidSubmit="HandleValidSubmit">
    <DataAnnotationsValidator />
    <ValidationSummary />

    <div class="mb-3">
        <label for="productName">Product Name</label>
        <InputText id="productName" class="form-control" @bind-Value="newProduct.Name" />
    </div>

    <div class="mb-3">
        <label for="productPrice">Price</label>
        <InputNumber id="productPrice" class="form-control" @bind-Value="newProduct.Price" />
    </div>

    <div class="mb-3">
        <label for="productDescription">Description</label>
        <InputTextArea id="productDescription" class="form-control" @bind-Value="newProduct.Description" />
    </div>
@* 
    <div class="mb-3">
        <label for="CategoryID">Category ID</label>
        <InputTextArea id="CategoryID" class="form-control" @bind-Value="newProduct.CatID" />
    </div> *@

@*     <div class="mb-3">
        <label for="sellerID">Seller ID</label>
        <InputTextArea id="sellerID" class="form-control" @bind-Value="newProduct.SellerID" />
    </div> 
 *@
    <button type="submit" class="btn btn-primary">Add Product</button>
</EditForm>

@code {
    private ProductInDTO newProduct = new ProductInDTO();

    private void HandleValidSubmit()
    {
        ProductService.AddProduct(newProduct);
        Navigation.NavigateTo("/products"); // Redirect to product list page (replace with actual route)
    }
}